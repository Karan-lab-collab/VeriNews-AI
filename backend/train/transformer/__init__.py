# backend/train/transformer/__init__.py
"""
VeriNews AI — Transformer training package (Milestone 2).

Modules
-------
config              Central hyperparameters and paths for DistilBERT.
dataset             Dataset loading, splitting, and PyTorch Dataset class.
train_distilbert    Training entry point (hardware-aware).
evaluate_distilbert Evaluation on test split, manual validation, NASA study.
predict_distilbert  Single-text inference (CLI + importable).
"""
